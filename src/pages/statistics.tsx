import { getLayoutWithSidebar } from 'layouts'

export default function PostsPage() {
  return <h2 className={'text-center py-[calc(50vh-100px)]'}>Will be soon...</h2>
}

PostsPage.getLayout = getLayoutWithSidebar
